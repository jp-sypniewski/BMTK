import { CompanyNameSearchPipe } from './company-name-search.pipe';

describe('CompanyNameSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CompanyNameSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
